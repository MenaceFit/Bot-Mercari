# Dépendances d'exécution. Installation : pip install -r requirements.txt
# (ou, mieux : pip install -e .  qui installe aussi les commandes
#  `buyee-radar`, `snipe` et `mercari-sniper`)

httpx[http2]>=0.27        # client asynchrone HTTP/2, connexions réutilisées
cryptography>=42.0        # signature DPoP (ES256) exigée par l'API Mercari
PyYAML>=6.0               # configuration
selectolax>=0.3.21        # parsing HTML des sources sans API publique

# API du dashboard temps réel (buyee-radar) et dashboard web de la v2
fastapi>=0.110
uvicorn[standard]>=0.29
